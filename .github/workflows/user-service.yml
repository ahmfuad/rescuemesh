name: User Service CI/CD

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'rescuemesh-user-service/**'
      - '.github/workflows/user-service.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'rescuemesh-user-service/**'
  workflow_dispatch:

env:
  SERVICE_NAME: user-service
  DOCKER_IMAGE: kdbazizul/rescuemesh-user-service
  NAMESPACE: rescuemesh

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.21'
        cache-dependency-path: rescuemesh-user-service/go.sum

    - name: Run tests
      working-directory: rescuemesh-user-service
      run: |
        go test -v ./...

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.DOCKER_IMAGE }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha,prefix={{branch}}-
          type=raw,value=latest,enable={{is_default_branch}}

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./rescuemesh-user-service
        file: ./rescuemesh-user-service/Dockerfile
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=registry,ref=${{ env.DOCKER_IMAGE }}:buildcache
        cache-to: type=registry,ref=${{ env.DOCKER_IMAGE }}:buildcache,mode=max

    - name: Set up kubectl
      uses: azure/setup-kubectl@v3
      with:
        version: 'latest'

    - name: Configure kubectl
      run: |
        mkdir -p $HOME/.kube
        echo "${{ secrets.KUBE_CONFIG }}" | base64 -d > $HOME/.kube/config
        chmod 600 $HOME/.kube/config

    - name: Deploy to Kubernetes
      if: github.ref == 'refs/heads/main'
      run: |
        kubectl set image deployment/${{ env.SERVICE_NAME }} \
          ${{ env.SERVICE_NAME }}=${{ env.DOCKER_IMAGE }}:latest \
          -n ${{ env.NAMESPACE }}
        
        kubectl rollout status deployment/${{ env.SERVICE_NAME }} -n ${{ env.NAMESPACE }} --timeout=5m

    - name: Verify deployment
      if: github.ref == 'refs/heads/main'
      run: |
        kubectl get pods -n ${{ env.NAMESPACE }} -l app=${{ env.SERVICE_NAME }}
        
        # Wait for pods to be ready
        kubectl wait --for=condition=ready pod \
          -l app=${{ env.SERVICE_NAME }} \
          -n ${{ env.NAMESPACE }} \
          --timeout=300s

    - name: Run smoke tests
      if: github.ref == 'refs/heads/main'
      run: |
        # Wait a bit for service to be fully ready
        sleep 10
        
        # Test health endpoint
        response=$(curl -s -o /dev/null -w "%{http_code}" https://api.villagers.live/health)
        if [ $response -ne 200 ]; then
          echo "Health check failed with status code: $response"
          exit 1
        fi
        echo "Health check passed!"

    - name: Rollback on failure
      if: failure() && github.ref == 'refs/heads/main'
      run: |
        kubectl rollout undo deployment/${{ env.SERVICE_NAME }} -n ${{ env.NAMESPACE }}
        echo "Deployment rolled back due to failure"

    - name: Notify on success
      if: success() && github.ref == 'refs/heads/main'
      run: |
        echo "✅ User Service deployed successfully!"
        echo "🔗 https://api.villagers.live/health"

    - name: Notify on failure
      if: failure()
      run: |
        echo "❌ User Service deployment failed!"
        echo "Check the logs above for details"
