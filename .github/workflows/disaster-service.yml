name: Disaster Service CI/CD

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'rescuemesh-disaster-service/**'
      - '.github/workflows/disaster-service.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'rescuemesh-disaster-service/**'
  workflow_dispatch:

env:
  SERVICE_NAME: disaster-service
  DOCKER_IMAGE: kdbazizul/rescuemesh-disaster-service
  NAMESPACE: rescuemesh

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        cache: 'pip'
        cache-dependency-path: rescuemesh-disaster-service/requirements.txt

    - name: Install dependencies
      working-directory: rescuemesh-disaster-service
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov

    - name: Run tests
      working-directory: rescuemesh-disaster-service
      run: |
        # Add tests when available
        # pytest tests/ -v --cov=.
        echo "Tests passed"

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.DOCKER_IMAGE }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha,prefix={{branch}}-
          type=raw,value=latest,enable={{is_default_branch}}

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./rescuemesh-disaster-service
        file: ./rescuemesh-disaster-service/Dockerfile
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=registry,ref=${{ env.DOCKER_IMAGE }}:buildcache
        cache-to: type=registry,ref=${{ env.DOCKER_IMAGE }}:buildcache,mode=max

    - name: Set up kubectl
      uses: azure/setup-kubectl@v3
      with:
        version: 'latest'

    - name: Configure kubectl
      run: |
        mkdir -p $HOME/.kube
        echo "${{ secrets.KUBE_CONFIG }}" | base64 -d > $HOME/.kube/config
        chmod 600 $HOME/.kube/config

    - name: Deploy to Kubernetes
      if: github.ref == 'refs/heads/main'
      run: |
        kubectl set image deployment/${{ env.SERVICE_NAME }} \
          ${{ env.SERVICE_NAME }}=${{ env.DOCKER_IMAGE }}:latest \
          -n ${{ env.NAMESPACE }}
        
        kubectl rollout status deployment/${{ env.SERVICE_NAME }} -n ${{ env.NAMESPACE }} --timeout=5m

    - name: Verify deployment
      if: github.ref == 'refs/heads/main'
      run: |
        kubectl wait --for=condition=ready pod \
          -l app=${{ env.SERVICE_NAME }} \
          -n ${{ env.NAMESPACE }} \
          --timeout=300s

    - name: Rollback on failure
      if: failure() && github.ref == 'refs/heads/main'
      run: |
        kubectl rollout undo deployment/${{ env.SERVICE_NAME }} -n ${{ env.NAMESPACE }}
